import { AbstractListPageService } from './list-page';

describe('AbstractListPageService', () => {
  it('should create an instance', () => {
    expect(AbstractListPageService).toBeTruthy();
  });
});
